# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Open source licenses list

When adding, removing, or upgrading npm dependencies, run `npm run licenses:generate` and commit the updated `src/generated/ossLicenses.json` (used by Settings → Open Source Licenses).
